/**
 * OsterdOps — Firebase Modules Entrypoint
 */

export * from "./config";
export * from "./client";
// Note: admin is imported explicitly from '@/lib/firebase/admin' in server contexts to preserve bundler separation.
